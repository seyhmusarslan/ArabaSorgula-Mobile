<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:grial="http://uxdivers.com/grial"
    xmlns:local="clr-namespace:arabasorgula"
    x:Class="arabasorgula.DocumentTimelineLeftItemTemplate">

    <Grid
        ColumnSpacing="0"
        RowSpacing="0"
        VerticalOptions="Start"
        ColumnDefinitions="Auto,*">

        <Grid
            Grid.Column="1"
            ColumnSpacing="0"
            RowSpacing="0"
            VerticalOptions="Start"
            Margin="40,20,20,0"
            RowDefinitions="*,*">

            <!-- AUTHOR -->
            <Label
                Grid.Row="0"
                TextColor="{ DynamicResource PrimaryColor }"
                VerticalOptions="Start"
                HorizontalOptions="Start"
                FontSize="12"
                Text="{ Binding Author, StringFormat='By {0:F0}' }"
            />

            <!-- DATE -->
            <Label
                Grid.Row="1"
                VerticalOptions="Start"
                TextColor="{ DynamicResource TextSecondaryColor }"
                FontSize="12"
                Text="{ Binding When }"
                HorizontalOptions="Start"
            />
        </Grid>

        <!-- BALLOON -->
        <Grid
            Grid.Row="0"
            Grid.Column="0"
            Padding="10"
            ColumnSpacing="0"
            VerticalOptions="Center"
            HorizontalOptions="End"
            Margin="{ OnIdiom
                Phone='10,10,6,10',
                Tablet='100,10,6,10'
            }"
            ColumnDefinitions="130,26">

            <BoxView
                Grid.Column="0"
                Style="{ StaticResource DocumentTimelineBubbleStyle }"
                Color="{ DynamicResource BackgroundSecondaryColor }"
            />

            <Label
                Margin="10"
                TextColor="{ DynamicResource TextColor }"
                FontSize="{ StaticResource BaseFontSize }"
                Text="{ Binding Title }"
                LineBreakMode="WordWrap"
            />

            <Label
                Grid.Row="0"
                Grid.Column="1"
                HorizontalOptions="Center"
                Text="{ x:Static local:GrialIconsFont.ShapeArrowRight }"
                TranslationX="-11"
                TextColor="{ DynamicResource BackgroundSecondaryColor }"
                Style="{ StaticResource FontIconFill }"
                FontSize="26"
            />
        </Grid>

        <!-- CONNECTION LINE -->
        <BoxView
            Grid.ColumnSpan="2"
            WidthRequest="3"
            VerticalOptions="Fill"
            HorizontalOptions="Center"
            Color="{ DynamicResource BackgroundSecondaryColor }"
        />

        <!-- ICON -->
        <local:RoundedIcon
            Grid.Column="0"
            Grid.Row="0"
            Grid.ColumnSpan="2"
            VerticalOptions="Start"
            HorizontalOptions="Center"
            Margin="0,16,0,0"
            HeightRequest="40"
            WidthRequest="40"
            IsIconBackgroundCircular="True"
            IconFontSize="20"
            IconText="{ Binding Icon }"
            IconBackgroundColor="{ grial:Colorize
                Key='DocumentTimeline',
                Value={Binding Icon}
            }"
            IconColor="White"
            IconPadding="3"
            IsCircular="True"
            BackgroundColor="{ DynamicResource BackgroundSecondaryColor }"
        />

    </Grid>

</ContentView>
