<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:grial="http://uxdivers.com/grial"
    xmlns:local="clr-namespace:arabasorgula"
    x:Class="arabasorgula.MainMenuItemTemplate">

    <Grid
        Padding="{ DynamicResource AirSpacingNoBorder }"
        ColumnDefinitions="30,*">

        <!-- ICON -->
        <Label
            Grid.Column="0"
            Text="{ Binding Icon }"
            HorizontalOptions="Start"
            Style="{ StaticResource MainMenuIconStyle }"
            VerticalTextAlignment="Center"
        />

        <!-- LABEL -->
        <Label
            Grid.Column="1"
            Text="{ Binding Name }"
            Margin="{ DynamicResource MainMenuFullLabelMargin }"
            Style="{ StaticResource MainMenuLabelStyle }"
            VerticalTextAlignment="Center"
        />

    </Grid>

</ContentView>