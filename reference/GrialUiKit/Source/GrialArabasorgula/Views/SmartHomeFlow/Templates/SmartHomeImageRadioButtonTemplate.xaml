<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:grial="http://uxdivers.com/grial"
    xmlns:local="clr-namespace:arabasorgula"
    x:Class="arabasorgula.SmartHomeImageRadioButtonTemplate">

    <RadioButton Value="{ Binding . }">
        <RadioButton.ControlTemplate>
            <ControlTemplate>
                <Grid
                    x:Name="checked"
                    Padding="4">
                    <grial:RoundedImage
                        Source="{ TemplateBinding BindingContext }"
                        CornerRadius="{ DynamicResource CornerRadius-x-sm }"
                        HeightRequest="45"
                        WidthRequest="45"
                    />

                    <!-- Check Icon -->
                    <!-- TODO: height = 1/2 Cornerradius -->
                    <local:RoundedIcon
                        x:Name="check"
                        Margin="0,-4,-4,0"
                        CornerRadius="8"
                        HeightRequest="16"
                        WidthRequest="16"
                        IconText="{ x:Static local:GrialIconsFont.Check }"
                        IconFontFamily="{ StaticResource GrialIconsFill }"
                        IconFontSize="12"
                        IconColor="{ DynamicResource BackgroundSecondaryColor }"
                        VerticalOptions="Start"
                        HorizontalOptions="End"
                        BackgroundColor="{ DynamicResource PrimaryColor }"
                    />
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CheckedStates">
                                <VisualState x:Name="Checked">
                                    <VisualState.Setters>
                                        <Setter TargetName="check" Property="IsVisible" Value="True" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Unchecked">
                                    <VisualState.Setters>
                                        <Setter TargetName="check" Property="IsVisible" Value="False" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>
                </Grid>
            </ControlTemplate>
        </RadioButton.ControlTemplate>
    </RadioButton>

</ContentView>

